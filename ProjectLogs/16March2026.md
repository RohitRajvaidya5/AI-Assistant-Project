# Daily Project Log

## Date
16 March 2026

## Summary of Work

Worked on improving the architecture and capabilities of the local AI assistant.

### Key Improvements

**Memory Storage System**
- Implemented a feature to store important user facts in a PostgreSQL database.
- Added trigger keywords (`remember`, `store`, `save`) to detect when information should be saved.
- Extracted and cleaned user input before storing it as a structured memory.
- Ensured only concise single-sentence memories are stored.

**Database Integration**
- Integrated database operations with the assistant.
- Implemented core functions:
  - `store_memory()` to save memories
  - `get_memories()` to retrieve stored data
  - `clear_whole_database()` to reset stored memories

**Logging System**
- Configured centralized logging for the project.
- Logs are now written to a dedicated `logs` folder instead of printing everything in the terminal.

**DeepSeek-Coder Model Setup**
- Installed Ollama and downloaded the **DeepSeek-Coder (6.7B)** model.
- Tested the model locally and integrated it with Python.
- Updated the assistant’s model stack to use:
  - **Llama3** for reasoning and general queries
  - **DeepSeek-Coder** for programming tasks
  - **Phi3** as a lightweight fallback model

**Version Control**
- Created a new feature branch: `feature/database_store` for memory storage and database-related changes.

---

## Learning

- Smaller models often produce unreliable structured outputs when extracting facts.
- Memory storage logic must be carefully designed to avoid storing incorrect information.
- Modular architecture (AI, database, commands, logging) improves maintainability and scalability.

---

## Next Steps

- Retrieve stored memories and inject them into the model prompt.
- Add commands like `show memories`.
- Implement intelligent model routing between Llama3, DeepSeek-Coder, and Phi3.
- Improve filtering so only meaningful long-term user facts are stored.