## Daily Progress Log

**Date:** 14 March 2026

### Work Done Today

- Continued improving my **local AI chatbot project using Python and Ollama**.
- Cleaned up the overall program structure to make the code more organized and easier to understand.
- Implemented a **command handler** to support commands like:
  - switching models (`phi3`, `llama3`, `tinyllama`)
  - resetting models
  - clearing the chat
- Added a **model fallback mechanism** so the program can switch models if one fails or gives a wrong answer.
- Implemented a **"wrong answer" handling feature** that automatically switches the model and retries.
- Added a **Google search fallback** when all models fail to provide a good answer.
- Implemented **terminal utilities** like clearing the terminal from inside the program.
- Experimented with **LLM-based intent detection** to determine whether the user wants to quit the program.
- Observed that small local models sometimes **hallucinate in intent classification**, which caused incorrect exits.
- Replaced LLM-based quit detection with a **simpler keyword-based exit logic** to make the program more reliable.
- Improved exit detection so that questions like  
  `why kishimoto quit naruto`  
  do not accidentally terminate the program.

### Key Learnings

- Using an LLM for simple logic (like exit detection) can introduce unnecessary complexity.
- Simple deterministic logic is often **more reliable and faster** for small tasks.
- Structuring code into small functions improves **readability and maintainability**.

### Next Steps

- Improve the chatbot by adding **streaming responses again**.
- Add **conversation memory support**.
- Add **extra assistant commands** (for example opening websites, searching YouTube, etc.).
- Continue refining the project and documenting progress.